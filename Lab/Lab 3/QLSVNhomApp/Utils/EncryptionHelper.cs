namespace QLSVNhomApp.Utils
{
    public static class EncryptionHelper
    {
        // Nếu cần xử lý mã hóa ở phía client, bạn có thể cài đặt các hàm tương ứng ở đây.
        // Hiện tại, mã hóa mật khẩu và điểm thi được thực hiện trong các stored procedure của SQL Server.
    }
}
